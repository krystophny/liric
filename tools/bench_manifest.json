{
  "schema_version": 1,
  "description": "Unified benchmark matrix manifest for bench_matrix.",
  "lanes": {
    "ir_file": {
      "description": "IR corpus lane using bench_corpus_compare (LIRIC vs real LLVM phases)",
      "baseline": "llvm",
      "required": true,
      "notes": "Expected to cover canonical corpus track with full attempted==completed in strict mode."
    },
    "api_e2e": {
      "description": "LFortran --jit end-to-end lane using bench_api",
      "baseline": "lfortran_llvm",
      "required": true,
      "notes": "Consumes compat_ll artifacts generated by bench_compat_check."
    },
    "micro_c": {
      "description": "TinyCC microbench lane using bench_tcc",
      "baseline": "tcc",
      "required": true,
      "notes": "Mandatory micro baseline for compile/reloc and wall-clock ratios."
    }
  },
  "modes": [
    "isel",
    "copy_patch",
    "llvm"
  ],
  "policy": {
    "unsupported": "hard_fail",
    "missing_cell": "hard_fail",
    "empty_dataset": "hard_fail"
  }
}
